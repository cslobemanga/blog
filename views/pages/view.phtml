<?php 
    $lang = $data['language'];
    
    $file_path  = LANG_PATH_FILES .DS. $lang .DS. $data['page']['Alias'] . '.txt';
     
    if( $text = file_get_contents( $file_path, FILE_USE_INCLUDE_PATH ) ) {

        echo $text; 
    
    } else {
        Session::setFlash( 'File not found: ' . $file_path, 'alert-warning' );
    }
 ?>